/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.istockphoto.com", "plus.unsplash.com", "images.unsplash.com"], // Adicione os domínios das imagens aqui
  },
};

module.exports = nextConfig;
